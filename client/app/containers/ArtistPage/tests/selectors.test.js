// import { fromJS } from 'immutable';
// import { makeSelectArtistPageDomain } from '../selectors';

// const selector = makeSelectArtistPageDomain();

describe('makeSelectArtistPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
