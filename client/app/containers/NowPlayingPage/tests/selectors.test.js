// import { fromJS } from 'immutable';
// import { makeSelectNowPlayingPageDomain } from '../selectors';

// const selector = makeSelectNowPlayingPageDomain();

describe('makeSelectNowPlayingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
